# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

Note: this project runs SDK 57 to match the team's installed Expo Go app version, which only
supports one SDK at a time. If Expo Go moves to a newer SDK later, re-run
`npm install expo@latest && npx expo install --fix` and bump this doc's version accordingly.
