# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

Note: this project was downgraded from SDK 57 to SDK 54 to match the team's installed Expo Go app
version, which only supports one SDK at a time. If Expo Go is updated to support a newer SDK later,
re-run `npm install expo@latest && npx expo install --fix` and bump this doc's version accordingly.
