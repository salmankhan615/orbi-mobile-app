In-app image/font assets imported directly by components (e.g. `import logo from '@/assets/images/logo.png'`).
Static assets referenced only from `app.json` (app icon, splash) stay in the root `/assets` folder.
