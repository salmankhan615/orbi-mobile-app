import { StyleSheet } from 'react-native';

// Shared style for the app's outermost container (used in App.tsx, outside src/theme's token system).
export const rootStyles = StyleSheet.create({
  flexFill: { flex: 1 },
});
